services:
  - type: web
    name: ecoflood-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    plan: free
    rootDir: .
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
